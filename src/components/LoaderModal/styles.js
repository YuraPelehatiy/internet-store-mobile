import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    loader: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
    },
    loaderText: {
        color: '#ff8a00',
        textAlign: 'center',
        fontSize: 30,
        alignSelf: 'stretch',
    },
});