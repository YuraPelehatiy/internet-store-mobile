import React from 'react';
import { View, Text } from 'react-native';
import s from './style';

const Logo = () => (
    <View style={s.container}>
        <Text style={s.text}>Internet Store</Text>
    </View>
);

export default Logo;